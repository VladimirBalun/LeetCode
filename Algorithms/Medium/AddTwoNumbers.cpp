#include <iostream>
#include <forward_list>

#include "../Helpers.hpp"

class Solution
{
public:
    ListNode* addTwoNumbers(const ListNode* l1, const ListNode* l2)
    {
        return nullptr;
    }
};

int main(int argc, char** argv)
{
    ListNode* ptr = nullptr;
    ASSERT_EQUALS(ptr, ptr);
}